import { ViewfilePDFPipe } from './viewfile-pdf.pipe';

describe('ViewfilePDFPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewfilePDFPipe();
    expect(pipe).toBeTruthy();
  });
});
